package com.example.offzmi.utils

import com.example.offzmi.R

class Const {

    companion object {
        const val BASE_URL = "${R.string.BASE_URL}"
        const val AIRTABLE_TOKEN = "${R.string.TOKEN}"
    }
}